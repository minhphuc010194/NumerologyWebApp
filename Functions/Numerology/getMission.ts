import { Data_numbers_Alphabets } from "../../data/mooks";
export const getMission = (fullName: string): number => {
   const name = fullName.toLocaleUpperCase();
   console.log(Data_numbers_Alphabets);
   return 0;
};
